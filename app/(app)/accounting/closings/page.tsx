import MacroClosingsView from '@/components/views/macro-closings-view';

export const metadata = {
  title: 'Fechos Globais - Contabilidade | FaturaAO',
};

export default function MacroClosingsPage() {
  return <MacroClosingsView />;
}
